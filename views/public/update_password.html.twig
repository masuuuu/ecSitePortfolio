<!DOCTYPE html>
<html lang="ja">
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="{{constant('models\\Bootstrap::APP_URL')}}assets/js/shopping.js"></script>
  <link href="{{constant('models\\Bootstrap::APP_URL')}}assets/css/style.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <title>パスワードの変更</title>
</head>
<body>
  <div class="wrapper">
    {% include 'header.html.twig' %}
    <div class="contents">
      <div class="contents_inner">
        <div class="main-contents">
        <h3>パスワードの変更</h3>
          <form action="" method="post">
            <table class="list detail block">
              <tr>
                <th>現在のパスワード</th>
                <td>
                  <input type="password" name="old_password" value="{{dataArr.old_password}}" size="16">
                  {% if errArr.old_password | length > 0 %}<br>
                    <span class="red">{{errArr.old_password}}</span>
                  {% endif %}
                </td>
              </tr>
              <tr>
                <th>新しいパスワード</th>
                <td>
                  <input type="password" name="new_password" value="{{dataArr.new_password}}" size="16">
                  {% if errArr.new_password | length > 0 %}<br>
                    <span class="red">{{errArr.new_password}}</span>
                  {% endif %}
                </td>
              </tr>
            </table>
            <div class="r-20"><input type="submit" name="reset" value="再設定"></div>
          </form>
          <div class="r-20"><a href="{{constant('models\\Bootstrap::ENTRY_URL')}}public/passwordResetRequestForm.php">パスワードを忘れた方はこちら</a></div>
          <div class="r-20"><a href="{{constant('models\\Bootstrap::ENTRY_URL')}}public/userInfo.php">戻る</a></div>
        </div>
      </div> 
    </div>
  </div>
</body>
</html>