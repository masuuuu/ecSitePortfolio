<!DOCTYPE html>
<html lang="ja">
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="{{constant('models\\Bootstrap::APP_URL')}}assets/js/shopping.js"></script>
  <link href="{{constant('models\\Bootstrap::APP_URL')}}assets/css/style.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <title>会員登録</title>
</head>
<body>
   <div class="wrapper">
      {% include 'header.html.twig' %}
      <div class="contents">
        <div class="contents_inner">
          <div class="main-contents">
          <h3>お支払い方法の変更</h3>
          <form action="" method="get">
          <table class="list detail">
            <tr>
              <th>お支払い方法</th>
            </tr>
            {% for val in paymentArr %}
            <tr>
              <td>
                <input type="radio" id="{{val.payment_name}}" name="payment" value="{{val.payment_id}}">
                <label for="{{val.payment_name}}">{{val.payment_name}}</label>
              </td>
            </tr>
            {% endfor %}
            </table>
            {% if payment_error | length > 0 %}<br>
              <span class="red">{{payment_error}}</span>
            {% endif %}
            <div class="c-20">
              <input type="submit" name="pay_ch" value="このお支払方法を使う">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>