<!DOCTYPE html>
<html lang="ja">
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="{{constant('models\\Bootstrap::APP_URL')}}assets/js/shopping.js"></script>
  <link href="{{constant('models\\Bootstrap::APP_URL')}}assets/css/style.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <title>パスワード再設定</title>
</head>
<body>
   <div class="wrapper">
      {% include 'header.html.twig' %}
      <div class="contents">
        <div class="contents_inner">
          <div class="main-contents">
          <h3>パスワードの再設定</h3>
          <form action="passwordResetConfirm.php" method="post">
            <table class="list detail">
              <tr>
                <th>パスワード<span class="red">必須</span></th>
                <td>
                  <input type="password" name="password" value="" size="40">
                  {% if errArr.password | length > 0 %}<br>
                    <span class="red">{{errArr.password}}</span>
                  {% endif %}
                </td>
              </tr>
              <tr>
                <th>パスワード(確認用)<span class="red">必須</span></th>
                <td>
                  <input type="password" name="password_con" value="" size="40">
                  {% if errArr.password_con | length > 0 %}<br>
                    <span class="red">{{errArr.password_con}}</span>
                  {% endif %}
                </td>
              </tr>
            </table>
            <div class="r-20">
              <input type="hidden" name="token" value="{{token}}">
              <input type="hidden" name="passwordResetToken" value="{{passwordResetToken}}">
              <input type="submit" name="send" value="送信">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>