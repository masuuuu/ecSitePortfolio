<!DOCTYPE html>
<html lang="ja">
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="{{constant('models\\Bootstrap::APP_URL')}}assets/css/style.css" type="text/css">
  <title>お問い合わせ詳細</title>
</head>
<body>
  <div class="wrapper">
    {% include 'header.html.twig' %}
    <div class="contents">
      <div class="contents_inner">
        <div class="admin-contents">
          <h3>お問い合わせ詳細</h3>
          <table class="list detail block">
            <tr>
              <th>お問い合わせID</th>
              <td>{{dataArr.contact_id}}</td>
            </tr>
            <tr>
              <th>お問い合わせ日時</th>
              <td>{{dataArr.regist_date}}</td>
            </tr>
            <tr>
              <th>お名前</th>
              <td>{{dataArr.name}}</td>
            </tr>
            <tr>
              <th>メールアドレス</th>
              <td>{{dataArr.email}}</td>
            </tr>
            <tr>
              <th>お問い合わせ内容</th>
              <td>{{dataArr.content}}</td>
            </tr>
          </table>
          <form action="" method="post">
          <div class="r-20">{% if dataArr.state == 0 %}<button type="submit" name="state" value="完了">対応済み</button>{% elseif dataArr.state == 1 %}このお問い合わせは対応済みです。{% endif %}</div>
          </form>
          <div class="r-20"><a href="{{constant('models\\Bootstrap::ENTRY_URL')}}admin/contactList.php">戻る</a></div>
        </div>
      </div>
    </div>
  </body>
</html>