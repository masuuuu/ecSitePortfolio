<div class="sp condition-paging">
  <ul>
  {% if pages > 0 %}
    {% if now_page >= 2 %}
      <li class="page"><a href='{{constant('models\\Bootstrap::ENTRY_URL')}}admin/itemList.php?page_id={{now_page-1}}&amp;limit={{limit}}&amp;sort={{sort}}{% if category_id | length > 0 %}&amp;category_id={{category_id}}{% endif %}'>&laquo;</a></li>
    {% else %}
      <li class="now-page">&laquo;</li>
    {% endif %}
    {% for i in 1..pages %}
      {% if i >= now_page - range and i <= now_page + range %}
        {% if i == now_page %}
          <li class="now-page">{{now_page}}</li>
        {% else %}
          <li class="page"><a href='{{constant('models\\Bootstrap::ENTRY_URL')}}admin/itemList.php?page_id={{page}}&amp;limit={{limit}}&amp;sort={{sort}}{% if category_id | length > 0 %}&amp;category_id={{category_id}}{% endif %}'>{{i}}</a></li>
        {% endif %}
      {% endif %}
    {% endfor %}
    {% if now_page < pages %}
      <li class="page"><a href='{{constant('models\\Bootstrap::ENTRY_URL')}}admin/itemList.php?page_id={{now_page+1}}&amp;limit={{limit}}&amp;sort={{sort}}{% if category_id | length > 0 %}&amp;category_id={{category_id}}{% endif %}'>&raquo;</a></li>
    {% else %}
      <li class="now-page">&raquo;</li>
    {% endif %}
  {% endif %}
  </ul>
</div>